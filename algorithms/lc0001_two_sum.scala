object Solution {
    def twoSum(nums: Array[Int], target: Int): Array[Int] = {

      nums
    }

    def main(args: Array[String]): Unit = {
      println("start calculating:")

        val input_arr = Array(1, 2, 3, 0, -2).sorted
        println(input_arr.indexOf(2))
        for (i <- input_arr) {
          println(i)
        }

    }
}
